name: gravity_server
description: Cloud application server for app Gravity
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
dependencies:
  hex: ^0.2.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  dart_jsonwebtoken: ^2.11.0
  graphql: ^5.1.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
